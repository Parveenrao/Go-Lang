package main

/* net\http is Go standard library for building web server and clients

   => Every go http server is based on
     
      func(w http.ResponseWriter , r*http request) 
	  
	  this is called handler 
	  w --> what you send back (response)
	  r --> what client sent (request) */

	  